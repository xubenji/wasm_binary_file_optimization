#!/bin/bash

# 判断输入参数是否为空
if [ -z "$1" ] || [ -z "$2" ] || [ -z "$3" ]
then
  echo "请提供需要读取的文件名、被替换的字符和替换的字符作为参数"
  exit 1
fi

# 判断文件是否存在
if [ ! -f "$1" ]
then
  echo "文件不存在"
  exit 1
fi

# 使用sed命令将文件中的指定字符替换为指定字符
sed -i "s/$2/$3/g" "$1"

# 输出结果
echo "文件$1中的$2已替换为$3"
